from zmq_client import ZMQClient

# Create a ZMQ client instance
client = ZMQClient()

# Print the connection info (just to verify)
print("ZMQ client initialized")

# You can add code to make a request here
# Example: response = client.req("test request")
# print(response)